# Security

Report vulnerabilities privately to **support@softwaretoaster.com**. Do not open issues containing API keys, payroll data, personal data, database dumps or exploitable production details.

The addon intentionally has no unauthenticated controller. Use a dedicated Odoo bot user, a short-lived API key where operationally possible, least-privilege company access and HTTPS. Imported entries remain drafts for human review.
