[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [".venv", "staffpass_payroll_connector/__manifest__.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]
